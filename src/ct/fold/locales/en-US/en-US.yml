fold:
  clap:
    fold_spaces: break lines at word boundaries rather than a hard cut-off
    fold_width: set WIDTH as the maximum line width rather than 80
  usage: fold [OPTION]... [FILE]...
  about: |-
    Writes each file (or standard input if no files are given)
    to standard output whilst breaking long lines
