[package]
name = "ct_libstdbuf"

version.workspace = true
authors.workspace = true
license.workspace = true
homepage.workspace = true
keywords.workspace = true
categories.workspace = true
edition.workspace = true

description = "stdbuf/libstdbuf ~ (ctutils); dynamic library required for stdbuf"
repository = "https://work.ctyun.cn/git/CTyunOS/syskits/src/branch/master/src/ct/stdbuf"

[lib]
name = "libstdbuf"
path = "src/libstdbuf.rs"
crate-type = [
  "cdylib",
  "rlib",
]

[dependencies]
cpp = "0.5"
libc = { workspace = true }

[build-dependencies]
cpp_build = "0.5"
